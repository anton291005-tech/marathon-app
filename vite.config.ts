/**
 * This app is bundled with Create React App (`react-scripts`), not Vite.
 *
 * Production web assets MUST land in `./dist/` (`BUILD_PATH=dist` — see `package.json` `build`
 * script and `.env.production`). Capacitor `webDir` is `./dist` (`capacitor.config.ts`).
 *
 * If you migrate to Vite, replace this stub with `defineConfig({ build: { outDir: 'dist' } })`
 * and remove CRA — until then, do not run `vite build` expecting this repo to build.
 */
export {};
